# 🎯 자바/코틀린 개발자를 위한 함수형 프로그래밍 가이드 - Part 1: 기초

> **"OOP는 움직이는 부품을 캡슐화해서 코드를 이해하기 쉽게 만들고, FP는 움직이는 부품 자체를 줄여서 코드를 이해하기 쉽게 만든다."** — Michael Feathers

---

## 📚 이 파트의 목차

1. [기초 체력 다지기](#1-기초-체력-다지기)
   - 순수 함수
   - 불변성
   - 참조 투명성
2. [함수를 값처럼 다루기](#2-함수를-값처럼-다루기)
   - 일급 함수
   - 고차 함수
   - 클로저
3. [함수 조립하기](#3-함수-조립하기)
   - 합성
   - 커링과 부분 적용

### 📖 전체 가이드 네비게이션

- **Part 1: 기초** (현재 문서) - 순수함수, 불변성, 고차함수, 합성, 커링
- [Part 2: 컨테이너/ADT](fp-guide-korean-part2.md) - 펑터, 모나드, 어플리커티브, 대수적 구조, ADT
- [Part 3: 평가/실전](fp-guide-korean-part3.md) - 지연 평가, 실전 예제, 퀴즈 정답

---

## 1. 기초 체력 다지기

### 1.1 순수 함수 (Pure Function)

#### 🎭 비유: 자판기

자판기를 떠올려보세요. 천 원 넣고 A1 버튼 누르면? 콜라 나옵니다. 또 천 원 넣고 A1 누르면? 또 콜라. 백 번을 해도 콜라입니다. 이게 바로 순수 함수의 핵심입니다.

**그림 1.1: 순수 함수 개념도**
```
┌─────────────────────────────────────────────────────────────────┐
│                      순수 함수 = 자판기                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   입력                              출력                         │
│   ────                              ────                         │
│   ₩1000 + A1 버튼  ──────────────→  콜라 (항상!)                │
│   ₩1000 + A1 버튼  ──────────────→  콜라 (항상!)                │
│   ₩1000 + A1 버튼  ──────────────→  콜라 (항상!)                │
│                                                                 │
│   ✓ 같은 입력 = 같은 출력 (결정적)                              │
│   ✓ 부작용 없음 (외부 세계를 건드리지 않음)                     │
│   ✓ 외부 상태에 의존하지 않음                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### ❌ 순수하지 않은 함수

```java
// src/main/java/com/example/fp/ImpureExample.java

public class ImpureExample {
    private int counter = 0;  // 외부 상태!

    // ❌ 불순: 외부 상태를 읽고 변경함
    public int addAndCount(int x) {
        counter++;                    // 부작용: 상태 변경
        System.out.println("호출됨"); // 부작용: I/O 발생
        return x + counter;           // 결과가 counter에 따라 달라짐
    }
}
```

호출할 때마다 결과가 달라집니다. 첫 번째 `addAndCount(5)`는 6을 반환하고, 두 번째는 7을 반환해요. 테스트하기 골치 아프죠.

#### ✅ 순수 함수

```java
// src/main/java/com/example/fp/PureExample.java

public class PureExample {

    // ✅ 순수: 같은 입력엔 항상 같은 출력
    public static int add(int a, int b) {
        return a + b;  // 부작용 없음, 결정적
    }

    // ✅ 순수: 변환만 하고 원본은 건드리지 않음
    public static List<Integer> doubleAll(List<Integer> numbers) {
        return numbers.stream()
                      .map(n -> n * 2)
                      .toList();  // 새로운 리스트 반환
    }
}
```

#### 코틀린 버전

```kotlin
// src/main/kotlin/com/example/fp/PureFunctions.kt

// ✅ 순수 함수 - 표현식 본문 사용
fun add(a: Int, b: Int): Int = a + b

// ✅ 순수 변환 - 원본 리스트 그대로
fun doubleAll(numbers: List<Int>): List<Int> = numbers.map { it * 2 }
```

#### 🧪 순수 함수 판별 체크리스트

| 질문 | 순수하려면? |
|------|-------------|
| 같은 입력 → 같은 출력? | 반드시 ✅ |
| 외부 상태 변경? | 반드시 ❌ |
| 외부 상태에 의존? | 반드시 ❌ |
| I/O 수행? | 반드시 ❌ |

#### 💡 실전 예제: 숙박 요금 계산

이커머스만이 아니라 여행/숙박 도메인에서도 순수 함수는 핵심입니다.

```java
// 순수 함수: 숙박 요금 계산
public record RoomPricing(BigDecimal basePrice, BigDecimal weekendSurcharge) {

    // ✅ 순수: 같은 입력(체크인/아웃, 룸타입)이면 항상 같은 가격
    public BigDecimal calculateTotal(
            LocalDate checkIn,
            LocalDate checkOut,
            int guestCount
    ) {
        long nights = ChronoUnit.DAYS.between(checkIn, checkOut);
        long weekendNights = countWeekendNights(checkIn, checkOut);

        return basePrice.multiply(BigDecimal.valueOf(nights))
                .add(weekendSurcharge.multiply(BigDecimal.valueOf(weekendNights)))
                .add(extraGuestFee(guestCount));
    }

    // ✅ 순수: 날짜만 보고 주말 수 계산
    private long countWeekendNights(LocalDate start, LocalDate end) {
        return start.datesUntil(end)
                .filter(d -> d.getDayOfWeek() == DayOfWeek.FRIDAY
                          || d.getDayOfWeek() == DayOfWeek.SATURDAY)
                .count();
    }
}

// 테스트가 쉬움! 목(mock) 없이 그냥 호출하면 됨
@Test
void 주말_포함_요금_계산() {
    var pricing = new RoomPricing(
        new BigDecimal("100000"),  // 기본가
        new BigDecimal("20000")    // 주말 추가
    );

    // 금요일~일요일 (2박, 주말 2일)
    var total = pricing.calculateTotal(
        LocalDate.of(2024, 1, 5),  // 금요일
        LocalDate.of(2024, 1, 7),  // 일요일
        2
    );

    assertEquals(new BigDecimal("240000"), total);  // 100000*2 + 20000*2
}
```

**그런데 현실에서 I/O 없이 어떻게 프로그램을 짜요?**

좋은 질문입니다. 함수형 프로그래밍은 "모든 코드를 순수하게" 만들라는 게 아닙니다. **순수한 부분과 불순한 부분을 명확히 분리**하라는 겁니다. 비즈니스 로직은 순수하게, I/O는 가장자리(edge)에서 처리하는 거죠.

---

### 1.2 불변성 (Immutability)

#### 🎭 비유: Git과 SVN

예전 SVN 쓸 때 기억나세요? 누가 파일 수정하면 이전 내용이 그냥 사라졌죠. 반면 Git은 모든 커밋이 히스토리로 남습니다. 불변성이 바로 이 Git 방식입니다.

**그림 1.2: 불변성 개념도 (Git vs SVN)**
```
┌─────────────────────────────────────────────────────────────────┐
│                      불변성 = Git 히스토리                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   가변 세계 (SVN 스타일):                                       │
│   ┌──────┐                                                      │
│   │ v1.0 │ ←── 누가 덮어씀. 이전 내용 날아감!                   │
│   └──────┘                                                      │
│                                                                 │
│   불변 세계 (Git 스타일):                                       │
│   ┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐                 │
│   │ v1.0 │───→│ v1.1 │───→│ v1.2 │───→│ v1.3 │                 │
│   └──────┘    └──────┘    └──────┘    └──────┘                  │
│      ↑           ↑           ↑           ↑                      │
│   모든 버전이 살아있음! 언제든 되돌릴 수 있음                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### Java: Record로 불변 객체 만들기 (Java 16+)

```java
// src/main/java/com/example/fp/model/User.java

// ✅ 설계 자체가 불변 - 모든 필드가 final
public record User(
    Long id,
    String name,
    String email,
    List<String> roles  // ⚠️ 주의: List 자체는 가변일 수 있음!
) {
    // 컴팩트 생성자에서 방어적 복사
    public User {
        roles = List.copyOf(roles);  // 진짜 불변으로 만들기
    }

    // "세터" 대신 새 인스턴스 반환
    public User withName(String newName) {
        return new User(id, newName, email, roles);
    }

    public User addRole(String role) {
        var newRoles = new ArrayList<>(roles);
        newRoles.add(role);
        return new User(id, name, email, newRoles);
    }
}
```

#### 코틀린: data class

코틀린이 이 부분에서 훨씬 편합니다.

```kotlin
// src/main/kotlin/com/example/fp/model/User.kt

data class User(
    val id: Long,
    val name: String,
    val email: String,
    val roles: List<String>  // 코틀린 List는 기본이 불변
) {
    // copy()로 "수정된 복사본" 만들기
    fun withName(newName: String): User = copy(name = newName)

    fun addRole(role: String): User = copy(roles = roles + role)
}

// 사용 예시
fun main() {
    val user1 = User(1, "김철수", "kim@email.com", listOf("USER"))
    val user2 = user1.addRole("ADMIN")  // user1은 그대로!

    println(user1.roles)  // [USER]
    println(user2.roles)  // [USER, ADMIN]
}
```

#### 🎯 왜 불변성이 중요한가?

```
┌─────────────────────────────────────────────────────────────────┐
│                     불변성의 이점                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 스레드 안전성                                               │
│     락 필요 없음 - 데이터가 안 바뀌니까!                        │
│     동시성 버그 원천 차단                                       │
│                                                                 │
│  2. 예측 가능성                                                 │
│     "이거 누가 바꿨지?" 디버깅 지옥 탈출                        │
│     한번 만든 객체는 영원히 그 상태                             │
│                                                                 │
│  3. 테스트 용이성                                               │
│     같은 입력 = 같은 출력, 항상                                 │
│     목(mock) 설정 단순해짐                                      │
│                                                                 │
│  4. 캐시 친화적                                                 │
│     안심하고 캐시 가능 - 값이 변할 일 없음                      │
│                                                                 │
│  5. 실행 취소/되돌리기 쉬움                                     │
│     이전 버전 그냥 들고 있으면 됨                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### 1.3 참조 투명성 (Referential Transparency)

#### 🎭 비유: 수학 시간의 대입

중학교 수학 시간 기억나세요? x = 5라고 하면, 어디서든 x를 5로 바꿔 써도 결과가 같았죠. 이게 참조 투명성입니다.

**그림 1.3: 참조 투명성 개념도**
```
┌─────────────────────────────────────────────────────────────────┐
│                참조 투명성 = 수학적 대입                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   수학에서 x = 5면, 어디든 x 대신 5 써도 됨:                    │
│                                                                 │
│   x + x = 5 + 5 = 10  ✅ 항상 맞음!                            │
│                                                                 │
│   FP에서 add(2,3) = 5면, 어디든 add(2,3) 대신 5 써도 됨:       │
│                                                                 │
│   add(2,3) + add(2,3) = 5 + 5 = 10  ✅ 항상 맞음!              │
│                                                                 │
│   이게 가능한 건 함수가 순수할 때만!                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```java
// ✅ 참조 투명
int result = add(2, 3) + add(2, 3);  // 5 + 5로 대체 가능

// ❌ 참조 투명하지 않음
int result = random.nextInt() + random.nextInt();  // 대체 불가!
```

#### 🎯 왜 참조 투명성이 중요한가?

```
┌─────────────────────────────────────────────────────────────────┐
│                 참조 투명성의 목적과 이점                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 로컬 추론 (Local Reasoning)                                 │
│     함수만 보고 동작 예측 가능                                  │
│     전역 상태, 실행 순서 추적 불필요                            │
│                                                                 │
│  2. 안전한 리팩토링 (Safe Refactoring)                          │
│     표현식 ↔ 값 자유롭게 치환 가능                             │
│     add(2,3)을 5로 바꿔도 프로그램 의미 변하지 않음            │
│                                                                 │
│  3. 컴파일러 최적화 (Compiler Optimization)                     │
│     같은 계산 캐싱 가능 (메모이제이션)                          │
│     실행 순서 재배치 가능 (병렬화)                              │
│                                                                 │
│  4. 테스트 용이 (Easy Testing)                                  │
│     입력만 주면 출력 검증 가능                                  │
│     테스트 순서, 환경에 무관                                    │
│                                                                 │
│  ═══════════════════════════════════════════════════════════    │
│                                                                 │
│  실전 예:                                                       │
│  ─────────────────────────────────────────────────────────────  │
│  add(2, 3) → 어디서 호출해도 5                                 │
│            → 인라인/아웃라인 자유로움                           │
│            → 캐싱해도 안전                                      │
│                                                                 │
│  getCurrentTime() → 매번 다른 값                               │
│                   → 치환 불가, 테스트 어려움                    │
│                   → 실행 순서에 민감                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

참조 투명성이 왜 중요하냐면, 컴파일러가 최적화하기 좋고, 개발자가 코드를 읽을 때 머릿속에서 "이건 그냥 5네" 하고 넘어갈 수 있기 때문입니다.

---

### 1.4 세 개념의 관계 정리

지금까지 배운 불변성, 순수 함수, 참조 투명성. 이 세 가지가 어떻게 연결되는지 정리해봅시다.

**그림 1.4: FP 기초 개념 관계도**
```
┌─────────────────────────────────────────────────────────────────┐
│                    FP 기초 개념 관계도                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   불변성 ─────────────→ 순수함수 ────────→ 참조투명성          │
│   (선행조건)            (핵심 특성)        (결과적 성질)        │
│                                                                 │
│   관계 설명:                                                    │
│   ─────────────────────────────────────────────────────────     │
│   • 불변성 → 순수함수 가능: 필요조건 (불변 없이 순수 어려움)   │
│   • 순수함수 → 참조투명성: 충분조건 (순수하면 참조투명)        │
│   • 참조투명 ≠> 순수함수: 메모이제이션 등 예외 존재            │
│                                                                 │
│   벤다이어그램:                                                 │
│   ┌─────────────────────────────────────────────┐               │
│   │ 참조투명한 표현식들                          │               │
│   │  ┌───────────────────────────────────┐      │               │
│   │  │ 순수 함수들                        │      │               │
│   │  │  ┌─────────────────────────┐      │      │               │
│   │  │  │ 불변 데이터만 사용하는   │      │      │               │
│   │  │  │ 순수 함수들 (이상적)    │      │      │               │
│   │  │  └─────────────────────────┘      │      │               │
│   │  └───────────────────────────────────┘      │               │
│   │     ↑ 메모이제이션된 함수 (순수는 아니지만  │               │
│   │       참조투명할 수 있음)                    │               │
│   └─────────────────────────────────────────────┘               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 🧭 실전 판별 Decision Tree

코드가 FP 원칙을 따르는지 판별하는 방법:

```
┌─────────────────────────────────────────────────────────────────┐
│                    FP 원칙 판별 흐름도                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Q1: 이 함수가 외부 상태를 읽거나 변경하나요?                   │
│      │                                                          │
│      ├── YES → ❌ 순수하지 않음                                │
│      │          (DB, 파일, 전역 변수, 시간, 난수 등)            │
│      │                                                          │
│      └── NO → Q2: 같은 인자로 호출하면 항상 같은 결과?         │
│                    │                                            │
│                    ├── NO → ❌ 순수하지 않음                   │
│                    │                                            │
│                    └── YES → Q3: 부작용(Side Effect) 있나요?   │
│                                   │                             │
│                                   ├── YES → ❌ 순수하지 않음   │
│                                   │         (로깅, 예외 등)     │
│                                   │                             │
│                                   └── NO → ✅ 순수 함수!       │
│                                                                 │
│  순수 함수면 → 자동으로 참조 투명!                             │
│  (표현식을 그 결과값으로 대체 가능)                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 💡 실전 예제

```java
// ✅ 불변 + 순수 + 참조투명 (이상적)
record Money(BigDecimal amount, Currency currency) {
    public Money add(Money other) {
        if (!this.currency.equals(other.currency)) {
            throw new IllegalArgumentException("통화 불일치");
        }
        return new Money(this.amount.add(other.amount), this.currency);
    }
}

// ⚠️ 참조투명하지만 엄밀히 순수하지 않은 경우 (메모이제이션)
class FibonacciMemoized {
    private final Map<Integer, Long> cache = new HashMap<>();  // 내부 상태!

    public long fib(int n) {
        return cache.computeIfAbsent(n, k ->
            k <= 1 ? k : fib(k - 1) + fib(k - 2)
        );
    }
    // 같은 n에 대해 항상 같은 결과 → 참조투명
    // 하지만 cache 변경 → 엄밀히 불순
    // 실용적으로는 "관찰 가능한 순수성" 인정
}

// ❌ 불순 + 참조투명하지 않음
class Counter {
    private int count = 0;

    public int increment() {  // 호출할 때마다 다른 값!
        return ++count;
    }
}
```

### 📝 1장 퀴즈

**퀴즈 1.1 (초급)**: 다음 중 순수 함수를 모두 고르세요.

```java
// A
int add(int a, int b) { return a + b; }

// B
int getCount() { return this.count++; }

// C
List<Integer> doubled(List<Integer> nums) {
    nums.replaceAll(n -> n * 2);
    return nums;
}

// D
String format(LocalDate date) {
    return date.format(DateTimeFormatter.ISO_DATE);
}

// E
int random() { return new Random().nextInt(); }
```

**퀴즈 1.2 (중급)**: 다음 코드가 참조 투명한지 판별하고 이유를 설명하세요.

```java
class Cache {
    private Map<String, Integer> cache = new HashMap<>();

    int compute(String key) {
        return cache.computeIfAbsent(key, k -> k.length() * 2);
    }
}
```

**퀴즈 1.3 (고급)**: 불변성과 순수 함수의 관계에 대해 설명하세요. 불변 데이터 없이 순수 함수를 작성할 수 있나요?

> 정답은 [Part 3: 퀴즈 정답](fp-guide-korean-part3.md#퀴즈-정답) 섹션에서 확인하세요.

---

## 2. 함수를 값처럼 다루기

### 2.1 일급 함수 (First-Class Function)

#### 🎭 비유: 함수도 그냥 값이다

자바에서 정수(int)로 뭘 할 수 있나요? 변수에 저장하고, 메서드에 넘기고, 메서드에서 반환하고, 컬렉션에 담을 수 있죠. **일급 함수란 함수도 이 모든 걸 할 수 있다**는 뜻입니다.

**그림 2.1: 일급 함수 개념도**
```
┌─────────────────────────────────────────────────────────────────┐
│                 일급 = 값처럼 다룰 수 있음                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   정수로 할 수 있는 것:                                         │
│   ┌─────────────────────────────────────────────────────┐       │
│   │ • 변수에 저장:      int x = 5;                      │       │
│   │ • 인자로 전달:      process(x);                     │       │
│   │ • 반환값으로 사용:  return x;                       │       │
│   │ • 컬렉션에 담기:    List<Integer>                   │       │
│   └─────────────────────────────────────────────────────┘       │
│                                                                 │
│   일급 함수도 이게 다 됨!                                       │
│   ┌─────────────────────────────────────────────────────┐       │
│   │ • 변수에 저장:      Function<A,B> f = x -> x*2;     │       │
│   │ • 인자로 전달:      process(f);                     │       │
│   │ • 반환값으로 사용:  return f;                       │       │
│   │ • 컬렉션에 담기:    List<Function<A,B>>             │       │
│   └─────────────────────────────────────────────────────┘       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```java
// src/main/java/com/example/fp/FirstClassDemo.java

import java.util.function.*;

public class FirstClassDemo {

    // 1️⃣ 함수를 변수에 저장
    Function<Integer, Integer> double_ = x -> x * 2;

    // 2️⃣ 함수를 컬렉션에 담기
    List<Function<Integer, Integer>> operations = List.of(
        x -> x + 1,
        x -> x * 2,
        x -> x * x
    );

    // 3️⃣ 함수를 인자로 받기
    public int apply(int value, Function<Integer, Integer> fn) {
        return fn.apply(value);
    }

    // 4️⃣ 함수를 반환하기
    public Function<Integer, Integer> multiplier(int factor) {
        return x -> x * factor;  // 함수를 반환!
    }
}
```

#### 2.1.1 람다 표현식 심층 이해

람다를 처음 볼 때 "이게 뭐지?" 싶죠. 핵심은 **"나중에 실행될 코드 조각"을 값처럼 전달**하는 겁니다.

```java
// 🔑 핵심 통찰: 람다는 정의 시점에 실행되지 않음!

// 넘기는 쪽 (Caller): "이 코드 조각을 나중에 실행해줘"
List<Integer> result = numbers.stream()
    .filter(n -> n > 3)          // 여기서 실행 안 됨!
    .map(n -> n * 2)             // 여기도 실행 안 됨!
    .toList();                   // 터미널 연산이 실행을 트리거

// 받는 쪽 (Callee - filter 내부):
for (T element : elements) {
    if (predicate.test(element)) {  // ← 이때 람다 실행!
        result.add(element);
    }
}
```

```
┌─────────────────────────────────────────────────────────────────┐
│                    람다 실행 시점 이해                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  정의 시점:   n -> n > 3                                       │
│              ↓                                                  │
│  "n을 받아서 n > 3 결과를 반환하는 코드" (아직 실행 X)         │
│                                                                 │
│  실행 시점:   predicate.test(5)  // true                       │
│              predicate.test(2)  // false                       │
│              ↓                                                  │
│  실제로 n에 5, 2가 바인딩되어 코드 실행                        │
│                                                                 │
│  왜 중요한가?                                                   │
│  • 지연 평가 가능 (필요할 때만 실행)                           │
│  • 무한 스트림 처리 가능                                       │
│  • 쇼트서킷 최적화 가능                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.1.2 Functional Interface 완전정복

자바에서 람다를 받으려면 **Functional Interface**가 필요합니다. 하나의 추상 메서드만 가진 인터페이스죠.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       핵심 Functional Interface 정리                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  인터페이스        │ 시그니처      │ 호출 메서드     │ 용도   │ 기억법     │
│  ─────────────────┼──────────────┼────────────────┼───────┼─────────────│
│  Function<T,R>    │ T → R        │ R apply(T)     │ 변환   │ "적용해서"  │
│  Consumer<T>      │ T → void     │ void accept(T) │ 소비   │ "받아서"    │
│  Supplier<T>      │ () → T       │ T get()        │ 생성   │ "얻기"      │
│  Predicate<T>     │ T → boolean  │ boolean test(T)│ 필터   │ "테스트"    │
│  UnaryOperator<T> │ T → T        │ T apply(T)     │ 변환   │ "적용해서"  │
│  BiFunction<T,U,R>│ (T,U) → R    │ R apply(T,U)   │ 변환   │ "적용해서"  │
│  BinaryOperator<T>│ (T,T) → T    │ T apply(T,T)   │ 결합   │ "적용해서"  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

```java
import java.util.function.*;

// 각 Functional Interface별 정의 → 호출 예시
public class FunctionalInterfaceDemo {

    public static void main(String[] args) {

        // Function<T,R>: T를 받아 R 반환 → .apply(T)
        Function<String, Integer> strLength = String::length;
        int len = strLength.apply("Hello");  // 5

        // Consumer<T>: T를 받아 소비 → .accept(T)
        Consumer<String> printer = System.out::println;
        printer.accept("Hello");  // 콘솔에 "Hello" 출력

        // Supplier<T>: 인자 없이 T 생성 → .get()
        Supplier<LocalDateTime> now = LocalDateTime::now;
        LocalDateTime time = now.get();  // 2024-01-15T10:30:00

        // Predicate<T>: T를 받아 boolean 반환 → .test(T)
        Predicate<Integer> isPositive = n -> n > 0;
        boolean result = isPositive.test(5);   // true
        boolean result2 = isPositive.test(-3); // false

        // UnaryOperator<T>: T를 받아 T 반환 → .apply(T)
        UnaryOperator<String> toUpper = String::toUpperCase;
        String upper = toUpper.apply("hello");  // "HELLO"

        // BiFunction<T,U,R>: T와 U를 받아 R 반환 → .apply(T,U)
        BiFunction<String, Integer, String> repeat = String::repeat;
        String repeated = repeat.apply("Ha", 3);  // "HaHaHa"

        // BinaryOperator<T>: T 두 개를 받아 T 반환 → .apply(T,T)
        BinaryOperator<Integer> add = Integer::sum;
        int sum = add.apply(3, 5);  // 8
    }
}
```

#### 2.1.3 메서드 레퍼런스 vs 람다

같은 동작을 두 가지로 표현할 수 있습니다. 언제 뭘 쓸까요?

```
┌─────────────────────────────────────────────────────────────────┐
│                   메서드 레퍼런스 vs 람다                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  상황                │ 람다                   │ 메서드 레퍼런스  │
│  ───────────────────┼───────────────────────┼───────────────── │
│  인스턴스 메서드     │ x -> x.toString()     │ Object::toString │
│  정적 메서드         │ x -> Integer.parseInt(x)│ Integer::parseInt│
│  생성자              │ () -> new User()      │ User::new        │
│  특정객체의 메서드   │ x -> list.contains(x) │ list::contains   │
│                                                                 │
│  💡 선택 기준:                                                  │
│  ─────────────────────────────────────────────────────────────  │
│  • 단순 위임 (인자 그대로 전달) → 메서드 레퍼런스 (더 명확)    │
│  • 변환/조합 필요 → 람다                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```java
// 메서드 레퍼런스가 더 좋은 경우 (단순 위임)
users.stream().map(User::getName)          // ✅ 간결
users.stream().map(user -> user.getName()) // ❌ 불필요하게 장황

// 람다가 더 좋은 경우 (변환/조합 필요)
users.stream().map(u -> u.getName() + "님")           // ✅ 변환 필요
users.stream().map(u -> String.format("%s님", u.getName())) // ✅ 복잡한 로직
```

---

### 2.2 고차 함수 (Higher-Order Function)

#### 🎭 비유: 쉐프와 레시피

일반 함수는 라인 쿡 같습니다. 재료 받아서 요리 내놓죠. 고차 함수는 총주방장입니다. **레시피 자체를 받아서 새로운 레시피를 만들어냅니다.**

**그림 2.2: 고차 함수 개념도**
```
┌─────────────────────────────────────────────────────────────────┐
│                 고차 함수 = 총주방장                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   일반 함수 (라인 쿡):                                          │
│   재료 받음 → 요리 반환                                         │
│   "계란 주세요" → "오믈렛 여기요"                               │
│                                                                 │
│   고차 함수 (총주방장):                                         │
│   레시피 받음 → 새 레시피 반환                                  │
│   "이 레시피 줄게" → "개선된 버전 여기"                         │
│                                                                 │
│   ┌──────────────────────────────────────────────────────┐      │
│   │  map():    "이 레시피를 모든 재료에 적용해"          │      │
│   │  filter(): "이 조건 통과하는 재료만 골라"            │      │
│   │  reduce(): "모든 재료를 하나로 합쳐"                 │      │
│   └──────────────────────────────────────────────────────┘      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 🎯 왜 함수를 인자로 받는가?

```
┌─────────────────────────────────────────────────────────────────┐
│                 고차 함수의 목적과 이점                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 행동의 추상화 (Behavior Abstraction)                        │
│     "무엇을 반복할지"와 "어떻게 반복할지" 분리                   │
│     반복 로직은 한 번만, 처리 로직은 호출자가 정의              │
│                                                                 │
│  2. 보일러플레이트 제거 (DRY)                                   │
│     같은 루프 구조 반복 없이 로직만 교체                        │
│     for 루프 10번 복사 → filter/map 한 번 + 람다               │
│                                                                 │
│  3. 의도 표현 (Expressive Intent)                               │
│     filter = "골라내다"                                         │
│     map = "변환하다"                                            │
│     reduce = "합치다"                                           │
│     코드를 읽으면 의도가 바로 보임!                             │
│                                                                 │
│  4. 테스트 용이                                                 │
│     고차 함수와 전달 함수 각각 독립적으로 테스트 가능           │
│                                                                 │
│  ═══════════════════════════════════════════════════════════    │
│                                                                 │
│  실제 문제 해결:                                                │
│  ─────────────────────────────────────────────────────────────  │
│  Before: 리스트 필터링 코드 10번 복붙                           │
│          → 버그 발견! 10곳 모두 수정해야 함                     │
│                                                                 │
│  After:  filter() 한 번 + 조건 람다                             │
│          → 조건만 바꾸면 됨, 반복 로직은 검증된 코드            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 삼신기: map, filter, reduce

함수형 프로그래밍의 삼대장입니다. 이 세 개만 잘 써도 대부분의 컬렉션 처리가 됩니다.

```java
// src/main/java/com/example/fp/HigherOrderDemo.java

public class HigherOrderDemo {

    record Order(Long id, String product, BigDecimal amount, boolean paid) {}

    public void demonstrate() {
        List<Order> orders = getOrders();

        // 고차 함수 실전 적용
        BigDecimal totalPaidRevenue = orders.stream()
            .filter(Order::paid)              // 1️⃣ filter: 결제된 것만
            .map(Order::amount)               // 2️⃣ map: 금액만 추출
            .reduce(BigDecimal.ZERO, BigDecimal::add);  // 3️⃣ reduce: 전부 합산
    }

    // 직접 만드는 고차 함수
    public <T> List<T> filterAndTransform(
            List<T> items,
            Predicate<T> condition,      // 함수를 인자로
            UnaryOperator<T> transform   // 함수를 인자로
    ) {
        return items.stream()
                    .filter(condition)
                    .map(transform)
                    .toList();
    }
}
```

#### 코틀린: 더 깔끔하게

```kotlin
// src/main/kotlin/com/example/fp/HigherOrderDemo.kt

data class Order(val id: Long, val product: String, val amount: BigDecimal, val paid: Boolean)

fun main() {
    val orders = getOrders()

    val totalPaidRevenue = orders
        .filter { it.paid }          // 후행 람다
        .map { it.amount }
        .reduce { acc, amount -> acc + amount }

    // 아니면 그냥 sumOf 쓰면 됨
    val total = orders.filter { it.paid }.sumOf { it.amount }
}

// 확장 함수로 고차 함수 만들기
inline fun <T> List<T>.filterAndTransform(
    condition: (T) -> Boolean,
    transform: (T) -> T
): List<T> = filter(condition).map(transform)
```

---

### 2.3 클로저 (Closure)

#### 🎭 비유: 기억을 담은 배낭

클로저는 **자기가 태어난 환경을 기억하는 함수**입니다. 마치 배낭에 추억을 담고 다니는 것처럼요.

**그림 2.3: 클로저 개념도**
```
┌─────────────────────────────────────────────────────────────────┐
│                클로저 = 배낭 멘 함수                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   클로저는 자기가 만들어진 환경의 변수들을 "기억"합니다.        │
│   마치 배낭에 추억을 담고 다니는 것처럼요.                      │
│                                                                 │
│   ┌─────────────────────────────────────────────────────┐       │
│   │  fun createCounter(): () -> Int {                   │       │
│   │      var count = 0  // 🎒 이게 배낭에 들어감        │       │
│   │      return { ++count }  // 배낭 메고 다님          │       │
│   │  }                                                  │       │
│   │                                                     │       │
│   │  val counter = createCounter()                      │       │
│   │  counter() // 1 (count 기억하고 있음!)              │       │
│   │  counter() // 2 (계속 기억!)                        │       │
│   └─────────────────────────────────────────────────────┘       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 🎯 왜 클로저가 유용한가?

```
┌─────────────────────────────────────────────────────────────────┐
│                    클로저의 목적과 이점                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 설정 캡슐화 (Encapsulated Configuration)                    │
│     팩토리 패턴 없이 "설정된 함수" 생성                         │
│     discountRate를 캡처한 할인 계산기                           │
│                                                                 │
│  2. 상태 은닉 (Hidden State)                                    │
│     클래스 없이도 private 변수 효과                             │
│     캡처된 변수는 외부에서 접근 불가                            │
│                                                                 │
│  3. 부분 적용의 기반 (Partial Application Base)                 │
│     인자 일부를 미리 고정한 함수 생성                           │
│     커링의 핵심 메커니즘                                        │
│                                                                 │
│  ═══════════════════════════════════════════════════════════    │
│                                                                 │
│  실제 활용 예:                                                  │
│  ─────────────────────────────────────────────────────────────  │
│  • 이벤트 핸들러: 컨텍스트 캡처한 콜백                          │
│    button.onClick(createHandler(userId));                       │
│                                                                 │
│  • 커스텀 비교자: 기준값 캡처한 Comparator                      │
│    createComparator(targetDate);                                │
│                                                                 │
│  • 로거 팩토리: 로그 레벨 캡처한 로거                           │
│    createLogger("ERROR");  // → 에러 전용 로거                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```java
// src/main/java/com/example/fp/ClosureDemo.java

public class ClosureDemo {

    // 클로저를 만들어내는 팩토리
    public static Supplier<Integer> createCounter() {
        // 이 변수가 클로저에 "캡처"됨
        final int[] count = {0};  // effectively final 우회

        return () -> ++count[0];  // 클로저: count를 "품고" 있음
    }

    // 더 실용적인 예: 설정값 캡처
    public static Function<BigDecimal, BigDecimal> createDiscountCalculator(
            BigDecimal discountRate  // 이게 캡처됨!
    ) {
        return price -> price.multiply(BigDecimal.ONE.subtract(discountRate));
    }

    public static void main(String[] args) {
        var counter = createCounter();
        System.out.println(counter.get());  // 1
        System.out.println(counter.get());  // 2

        var tenPercentOff = createDiscountCalculator(new BigDecimal("0.10"));
        var twentyPercentOff = createDiscountCalculator(new BigDecimal("0.20"));

        System.out.println(tenPercentOff.apply(new BigDecimal("100")));   // 90
        System.out.println(twentyPercentOff.apply(new BigDecimal("100"))); // 80
    }
}
```

#### 2.3.1 Effectively Final과 흔한 실수

자바에서 람다가 외부 변수를 캡처하려면 그 변수가 **effectively final**이어야 합니다. 왜 이런 제약이 있을까요?

```java
// ❌ 흔한 실수: 루프에서 람다 캡처
ExecutorService executor = Executors.newFixedThreadPool(4);

for (int i = 0; i < 10; i++) {
    executor.submit(() -> System.out.println(i));  // 컴파일 에러!
    // error: local variables referenced from a lambda expression
    //        must be final or effectively final
}

// 왜 에러일까?
// 1. i가 루프마다 변경됨 → effectively final이 아님
// 2. 람다가 실행될 때 i가 어떤 값인지 보장할 수 없음
// 3. 멀티스레드 환경에서 동기화 문제 발생 가능
```

```
┌─────────────────────────────────────────────────────────────────┐
│              왜 Effectively Final이 필요한가?                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  시나리오: i=0 일 때 람다 생성, i=5 일 때 람다 실행             │
│                                                                 │
│  만약 i가 변경 가능하다면?                                      │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  시점1: i=0, 람다 생성 → 람다가 i를 "캡처"             │    │
│  │  시점2: i=5, 다른 스레드에서 람다 실행                   │    │
│  │         → i가 0일까? 5일까? 불명확!                     │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  FP 원칙과의 연결:                                              │
│  • 불변성: 캡처된 값이 변하면 순수성 깨짐                       │
│  • 참조 투명성: 같은 람다가 다른 결과를 낼 수 있음             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```java
// ✅ 해결책 1: 루프 변수를 로컬 final로 복사
for (int i = 0; i < 10; i++) {
    final int capturedI = i;  // 이 값은 루프마다 새로 만들어짐
    executor.submit(() -> System.out.println(capturedI));
}

// ✅ 해결책 2: IntStream 사용 (더 함수형)
IntStream.range(0, 10)
    .forEach(i -> executor.submit(() -> System.out.println(i)));
    // forEach의 i는 각 반복마다 새 값!
```

### 📝 2장 퀴즈

**퀴즈 2.1 (초급)**: 다음 Functional Interface의 시그니처를 완성하세요.

```java
// Predicate<T>의 시그니처는?
// Consumer<T>의 시그니처는?
// Supplier<T>의 시그니처는?
// BiFunction<T, U, R>의 시그니처는?
```

**퀴즈 2.2 (중급)**: 다음 코드의 출력 결과를 예측하세요.

```java
List<String> names = List.of("Alice", "Bob", "Charlie");
Function<String, Integer> getLength = String::length;
Consumer<Integer> printIt = System.out::println;

names.stream()
    .map(getLength)
    .forEach(printIt);
```

**퀴즈 2.3 (고급)**: 클로저가 "effectively final" 변수만 캡처할 수 있는 이유를 FP 원칙 관점에서 설명하세요.

> 정답은 [Part 3: 퀴즈 정답](fp-guide-korean-part3.md#퀴즈-정답) 섹션에서 확인하세요.

---

## 3. 함수 조립하기

### 3.1 합성 (Composition)

#### 🎭 비유: 유닉스 파이프

리눅스에서 `cat file.txt | grep "error" | sort | uniq` 써보셨죠? 작은 명령어들을 파이프로 연결해서 복잡한 작업을 하는 거예요. 함수 합성이 딱 이겁니다.

**그림 3.1: 함수 합성 개념도**
```
┌─────────────────────────────────────────────────────────────────┐
│                   합성 = 유닉스 파이프                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Unix: cat file.txt | grep "error" | sort | uniq              │
│                                                                 │
│   FP:   readFile.andThen(filterErrors).andThen(sort).andThen(unique)│
│                                                                 │
│   ┌─────┐    ┌─────┐    ┌─────┐    ┌─────┐                     │
│   │  f  │───→│  g  │───→│  h  │───→│  i  │                     │
│   └─────┘    └─────┘    └─────┘    └─────┘                      │
│                                                                 │
│   f.andThen(g).andThen(h).andThen(i) = i(h(g(f(x))))          │
│                                                                 │
│   핵심: 각 함수는 자기 입출력만 신경 쓰면 됨                    │
│   앞뒤로 누가 오든 상관없음!                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 🎯 왜 합성하는가?

```
┌─────────────────────────────────────────────────────────────────┐
│                  함수 합성의 목적과 이점                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 단일 책임 (Single Responsibility)                           │
│     각 함수는 하나의 작업만 담당 → 이해/테스트 용이              │
│     trim()은 공백 제거만, lowercase()는 소문자 변환만            │
│                                                                 │
│  2. 재사용성 (Reusability)                                      │
│     trim, lowercase는 slugify 외에도 어디서든 활용 가능         │
│     작은 조각이니까 다른 곳에서 재조합 가능                      │
│                                                                 │
│  3. 디버깅 용이 (Easy Debugging)                                │
│     파이프라인 중간에 로그 함수 삽입 가능                       │
│     trim.andThen(log).andThen(lowercase) → 중간 상태 확인       │
│                                                                 │
│  4. 유연한 조립 (Flexible Assembly)                             │
│     순서 변경, 함수 추가/제거가 쉬움                            │
│     새 요구사항? → 함수 하나 끼워넣으면 됨                      │
│                                                                 │
│  5. 선언적 코드 (Declarative Code)                              │
│     "무엇을 하는지" 명확히 읽힘                                 │
│     trim.andThen(lowercase).andThen(removeSpaces)               │
│     → "공백 제거 후 소문자로 바꾸고 공백을 대시로"              │
│                                                                 │
│  ═══════════════════════════════════════════════════════════    │
│                                                                 │
│  실제 문제 해결:                                                │
│  ─────────────────────────────────────────────────────────────  │
│  Before: 500줄짜리 메서드                                       │
│          → 어디서 버그? 전체를 읽어야 함                        │
│          → 테스트? 모킹 지옥                                    │
│                                                                 │
│  After:  10개의 작은 순수 함수                                  │
│          → 단위별 테스트, 단위별 디버깅                         │
│          → 필요한 함수만 재조합                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```java
// src/main/java/com/example/fp/CompositionDemo.java

public class CompositionDemo {

    // 작고 단순한 함수들
    Function<String, String> trim = String::trim;
    Function<String, String> lowercase = String::toLowerCase;
    Function<String, String> removeSpaces = s -> s.replace(" ", "-");

    // 조립해서 슬러그 생성기 완성
    Function<String, String> slugify = trim
            .andThen(lowercase)
            .andThen(removeSpaces);

    public void demo() {
        String title = "  Hello World Example  ";
        System.out.println(slugify.apply(title));  // "hello-world-example"
    }
}
```

#### 3.1.1 Function.andThen() 이해하기

`andThen`이 어떻게 동작하는지 내부를 들여다봅시다.

```java
// Function 인터페이스의 실제 구현 (단순화)
@FunctionalInterface
public interface Function<T, R> {
    R apply(T t);  // 추상 메서드 (딱 하나!)

    // andThen: 디폴트 메서드!
    default <V> Function<T, V> andThen(Function<? super R, ? extends V> after) {
        return (T t) -> after.apply(apply(t));
        //              ↑           ↑
        //              │           └── 1. 먼저 this.apply(t) 실행
        //              └── 2. 그 결과를 after.apply()에 전달
    }

    // compose: andThen의 반대 방향
    default <V> Function<V, R> compose(Function<? super V, ? extends T> before) {
        return (V v) -> apply(before.apply(v));
        //                          ↑ before 먼저 실행
    }
}
```

```
┌─────────────────────────────────────────────────────────────────┐
│                 andThen vs compose 비교                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  f.andThen(g):  입력 → f → g → 출력                           │
│                 "f 다음에 g" (왼쪽→오른쪽, 자연스러움)          │
│                                                                 │
│  f.compose(g):  입력 → g → f → 출력                           │
│                 "g 먼저, 그 다음 f" (수학 함수 합성 순서)       │
│                                                                 │
│  추천: andThen이 읽기 쉬움! (데이터 흐름 순서)                 │
│                                                                 │
│  핵심 통찰:                                                     │
│  ─────────────────────────────────────────────────────────────  │
│  • andThen은 새 Function을 반환 - 원본 불변!                   │
│  • 각 함수는 독립적 - 앞뒤 관계 몰라도 됨                      │
│  • 타입만 맞으면 어떤 함수든 조립 가능                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.2 커링과 부분 적용

#### 🎭 비유: 커피 주문서

스타벅스에서 주문할 때 한 번에 "벤티 오트밀크 바닐라 라떼"라고 하죠. 그런데 **커링**은 주문서를 한 칸씩 채워나가는 겁니다.

**그림 3.2: 커링 개념도**
```
┌─────────────────────────────────────────────────────────────────┐
│              커링 = 주문서 한 칸씩 채우기                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   일반 함수 (한 번에 다):                                       │
│   orderCoffee(size, milk, syrup) → 커피                        │
│                                                                 │
│   커링된 함수 (한 칸씩):                                        │
│   orderCoffee(size) → milk 받는 함수 반환                      │
│                       → syrup 받는 함수 반환                   │
│                       → 커피                                   │
│                                                                 │
│   orderCoffee("벤티")("오트")("바닐라") → 커피                 │
│        │                │         │                             │
│        └── 1단계        └── 2단계   └── 3단계                  │
│                                                                 │
│   부분 적용:                                                    │
│   largeOrder = orderCoffee("벤티")     // 사이즈 고정!         │
│   largeOatOrder = largeOrder("오트")   // 밀크도 고정!         │
│   myCoffee = largeOatOrder("바닐라")   // 최종 주문            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 🎯 왜 커링/부분적용을 사용하는가?

```
┌─────────────────────────────────────────────────────────────────┐
│               커링/부분적용의 목적과 이점                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 재사용 가능한 특수화 (Reusable Specialization)              │
│     범용 함수 → 특수 함수 파생                                  │
│     log(level, msg) → infoLog = log("INFO")                    │
│                                                                 │
│  2. 합성 적합성 (Composition Ready)                             │
│     단일 인자 함수로 변환 → 파이프라인 조립 용이               │
│     f(a,b).andThen(g) → 불가                                   │
│     curry(f)(a).andThen(g) → 가능!                             │
│                                                                 │
│  3. 의존성 주입 스타일 (DI Style)                               │
│     설정/서비스를 먼저 주입, 데이터는 나중에                    │
│     createService(config)(data) 패턴                            │
│                                                                 │
│  ═══════════════════════════════════════════════════════════    │
│                                                                 │
│  실제 활용 예:                                                  │
│  ─────────────────────────────────────────────────────────────  │
│  • 로깅: log(level)(message)                                   │
│          infoLog = log("INFO")                                 │
│          errorLog = log("ERROR")                               │
│                                                                 │
│  • 할인 계산: discount(rate)(price)                            │
│              회원할인 = discount(0.1)                          │
│              VIP할인 = discount(0.2)                           │
│                                                                 │
│  • 검증기: validate(rules)(data)                               │
│            userValidator = validate(userRules)                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```java
// src/main/java/com/example/fp/CurryingDemo.java

public class CurryingDemo {

    // 일반 함수: 인자 한 번에 다 받음
    public static BigDecimal calculatePrice(String size, String milk, String syrup) {
        // ... 계산
        return BigDecimal.ZERO;
    }

    // 커링된 버전: 인자 하나씩
    public static Function<String, Function<String, Function<String, BigDecimal>>>
            calculatePriceCurried() {
        return size -> milk -> syrup -> {
            // ... 세 값 모두 사용해서 계산
            return BigDecimal.ZERO;
        };
    }

    public static void main(String[] args) {
        var curriedPrice = calculatePriceCurried();

        // 부분 적용: 자주 쓰는 조합을 미리 만들어둠
        var largePriceCalculator = curriedPrice.apply("Large");
        var largeSoyPriceCalculator = largePriceCalculator.apply("Soy");

        // 이제 시럽만 정하면 되는 함수가 됨
        BigDecimal price = largeSoyPriceCalculator.apply("Vanilla");
    }
}
```

#### 코틀린: 훨씬 깔끔

```kotlin
// src/main/kotlin/com/example/fp/CurryingDemo.kt

// 확장 함수로 커링 구현
fun <A, B, C> ((A, B) -> C).curry(): (A) -> (B) -> C = { a -> { b -> this(a, b) } }

// 사용
val add: (Int, Int) -> Int = { a, b -> a + b }
val curriedAdd = add.curry()

val addFive = curriedAdd(5)  // 부분 적용!
println(addFive(3))   // 8
println(addFive(10))  // 15

// 실용 예: 로거 팩토리
val log: (String) -> (String) -> Unit = { level -> { message ->
    println("[$level] $message")
}}

val info = log("INFO")
val error = log("ERROR")

info("서버 시작됨")      // [INFO] 서버 시작됨
error("연결 끊김")       // [ERROR] 연결 끊김
```

#### 3.2.1 커링 문법 완전 해부

커링된 함수의 타입 선언이 복잡해 보이죠. 하나씩 뜯어봅시다.

```java
// 이 타입이 뭘 의미하는지 분해해봅시다
Function<String, Function<String, Function<String, BigDecimal>>>
//       │              │              │              │
//       │              │              │              └─ 최종 반환값
//       │              │              └─ 3번째 인자 (syrup)
//       │              └─ 2번째 인자 (milk)
//       └─ 1번째 인자 (size)

// 화살표 표기로 보면:
// String → (String → (String → BigDecimal))
// 오른쪽 결합! (괄호 생략 가능)
// String → String → String → BigDecimal
```

```
┌─────────────────────────────────────────────────────────────────┐
│                 커링 타입 시각화                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Function<A, Function<B, Function<C, D>>>                      │
│                                                                 │
│  호출 1: f.apply("Large")                                      │
│          │                                                      │
│          ↓                                                      │
│  Function<String, Function<String, BigDecimal>>                │
│  "Large를 기억하는" 새 함수                                    │
│                                                                 │
│  호출 2: .apply("Soy")                                         │
│          │                                                      │
│          ↓                                                      │
│  Function<String, BigDecimal>                                  │
│  "Large와 Soy를 기억하는" 새 함수                              │
│                                                                 │
│  호출 3: .apply("Vanilla")                                     │
│          │                                                      │
│          ↓                                                      │
│  BigDecimal                                                     │
│  최종 결과!                                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```java
// 람다 구현도 오른쪽 결합
size -> milk -> syrup -> calculate(size, milk, syrup)
// ↑        ↑        ↑
// │        │        └─ size, milk 캡처, syrup 받아 계산
// │        └─ size 캡처, milk 받아 "함수 반환"
// └─ size 받아 "함수 반환"

// 풀어 쓰면 이렇게 됨
size -> {
    return milk -> {
        return syrup -> {
            return calculate(size, milk, syrup);
        };
    };
};

// 각 단계에서 이전 인자들은 클로저로 캡처됨!
```

### 📝 3장 퀴즈

**퀴즈 3.1 (초급)**: `f.andThen(g)`와 `f.compose(g)`의 차이점을 설명하세요.

**퀴즈 3.2 (중급)**: 다음 코드의 출력을 예측하세요.

```java
Function<Integer, Integer> addOne = x -> x + 1;
Function<Integer, Integer> multiplyTwo = x -> x * 2;
Function<Integer, Integer> combined = addOne.andThen(multiplyTwo);

System.out.println(combined.apply(5));
```

**퀴즈 3.3 (고급)**: 다음 커링된 함수의 타입을 작성하고, 부분 적용 예시를 보이세요.

```java
// 세 문자열을 받아서 이메일 주소를 생성하는 함수를 커링하세요
// createEmail("user", "domain", "com") → "user@domain.com"
```

> 정답은 [Part 3: 퀴즈 정답](fp-guide-korean-part3.md#퀴즈-정답) 섹션에서 확인하세요.

---

## 📖 다음 단계

이제 기초를 다졌습니다! 다음 Part에서는 더 강력한 도구들을 배웁니다.

**[➡️ Part 2: 컨테이너/ADT](fp-guide-korean-part2.md)**로 계속
- 펑터 (Functor) - map의 정체
- 모나드 (Monad) - flatMap의 정체
- 어플리커티브 (Applicative) - 독립 계산 결합
- 대수적 구조 - 세미그룹과 모노이드
- 대수적 데이터 타입 (ADT) - 곱 타입과 합 타입

---

### 📖 전체 가이드 네비게이션

- **Part 1: 기초** (현재 문서) - 순수함수, 불변성, 고차함수, 합성, 커링
- [Part 2: 컨테이너/ADT](fp-guide-korean-part2.md) - 펑터, 모나드, 어플리커티브, 대수적 구조, ADT
- [Part 3: 평가/실전](fp-guide-korean-part3.md) - 지연 평가, 실전 예제, 퀴즈 정답
